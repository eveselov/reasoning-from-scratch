# Chapter 02.Generating text with ap retrained LLM

**My own notes; not for Amazon review. See Amazon review in review/notes.md**

## Multi-step reasoning in one inference cycle

**Q:** *Please confirm my understanding of how Quen3 reasoning model works here - in just one act of inference or like an agent with seveal sequentially generated prompts? If the whole response is just one autogeneration, then how it looks like multi-step thinking - as if the model first planned, then executed on the plan? Would it be true to say that the model uses its own growing context window for creating plans and enerating/consuming intermediary results - all within one autogeneration loop?*

**Claude:** Yes, your understanding is correct. It's one act of inference: one prompt goes in and one autoregressive loop runs until the end-of-text token. There's no agent loop, no re-prompting and no outside controller. The "plan → execute → check" behaviour is just text the model writes into its own context and then reads back.

**What the code does in [generate_simple.py](ch02/05_use_model/generate_simple.py):**

1. The tokenizer wraps your question in the chat template ([qwen3.py:407-413](reasoning_from_scratch/qwen3.py#L407-L413)):
   ```
   <|im_start|>user
   Find all c in Z_3 ...<|im_end|>
   <|im_start|>assistant
   ```
2. That becomes one tensor of token IDs, which goes into `generate_text_basic_stream_cache` ([line 119](ch02/05_use_model/generate_simple.py#L119)).
3. The loop predicts one token, appends it, predicts the next, and so on until `eos_token_id` or `max_new_tokens`. That's all it does.

The output then looks like `<think> ...long reasoning... </think> final answer <|im_end|>`, but it all comes from that one loop. Nothing in the code treats the thinking part differently from the answer part. The `<think>` tags are ordinary tokens the model chooses to emit.

**How `add_thinking` works.** `add_thinking=True` turns thinking on by *leaving out* any `<think>` tag, so the model is free to open one itself. `add_thinking=False` pre-fills an empty `<think>\n\n</think>` block ([line 413](reasoning_from_scratch/qwen3.py#L413)). The model then acts as if it has already finished thinking and goes straight to the answer. So thinking mode is switched on and off purely by what text sits in the prompt, not by any setting in the model.

**Why one loop can look like multi-step thinking.** Every new token is computed with attention over *all* earlier tokens: the prompt plus everything the model has written so far. So:
- When it writes "First, let me check c = 0…", that text becomes input for every later token.
- A partial result like "x² + 1 has no root in Z₃" is now in the context, and later tokens can build on it.
- A "Wait, let me double-check…" is the model reading its own earlier output and reacting to it.

So yes: the growing context window is the model's scratchpad and working memory. Plans and intermediate results only exist as tokens in that context. There's no hidden plan stored anywhere else. The KV cache doesn't change this. It only saves recomputing attention over the tokens already in the context; it adds no memory of its own.

**Two refinements to your mental model:**
- **There's no separate planning mechanism.** The same weights and the same next-token step produce the plan, the steps and the answer. The model writes plan-like text because it was trained to produce that kind of step-by-step trace. That training is what makes this "reasoning model" differ from the base model with the same architecture. How the training is done comes in later chapters, so I won't go into it here.
- **The written plan isn't a binding program.** The model tends to follow what it wrote, because that text shapes what comes next, but nothing enforces it. It can drift from its plan, and the visible reasoning doesn't always match the computation that actually produced the answer.

**How this differs from an agent.** An agent has an outer loop in ordinary code. It calls the model, reads the output, maybe runs a tool (search, a calculator, code), and builds a *new* prompt with the results for another call. That's several generations with outside information added between them. Here it's one generation and no outside information: the model only ever sees the original prompt plus its own tokens.

A good one-line summary for your friends talk: *"A reasoning model does not think and then answer. It thinks out loud, and hearing itself is how it thinks."*

**Q:** *Thanks! I suspected that but was not sure. What about this thread with you itself. I assume that it is not a single inference run: using tools would be impossible, right?*

**Claude:** You're right. This thread is the agent pattern from my last answer, not one inference run.

**What happens when I use a tool.** Inside one generation I work much like Qwen3: one token at a time, with my own reasoning written into the context. The difference is that I can emit a structured tool-call block, for example "Read `qwen3.py`, lines 430–480". When that happens:

1. Generation **stops** at the end of the tool call.
2. The Claude Code harness, which is ordinary software running on your machine, reads the call and actually runs it (reads the file, runs Grep, etc.).
3. The harness adds the tool's output to the conversation and **calls the model again** with everything so far: system prompt, CLAUDE.md, your messages, my earlier outputs, the tool results.
4. I continue from there. I might call another tool, or write the final answer.

So one reply from me can be several inference runs. In this thread:
- **Your first question (downloads):** Grep → stop → run → two Reads in parallel → stop → run → one more Read → stop → run → final answer. That's about 4 generation calls.
- **Your second question (reasoning):** Read `generate_simple.py` → Grep the chat template → answer. That's about 3 calls. The `add_thinking` detail came from the Grep result. I couldn't have seen it in a single pass.
- **This answer:** one call, no tools.

**Why tools need the stop.** A single autoregressive loop can only condition on what's already in its context. It can *write* "let me read the file", but the file's contents aren't there, so it could only guess them. Getting real outside information means pausing generation, letting something outside the model fetch it, and continuing with the result in the context. That pause-and-resume is exactly what an agent loop does.

**Between your messages the model keeps no state.** I don't "remember" the earlier turns. Each call resends the whole conversation, and I read it fresh. The provider can cache the processed prefix (prompt caching), which is basically your KV cache kept on the server, so the history doesn't have to be recomputed every time. That's a speed-up, not memory. When a conversation gets very long, the harness can summarize older parts to make room. Memory that lasts across threads, like CLAUDE.md or my memory files, is just text loaded back into the context at the start.

**My thinking works like Qwen3's.** When I reason before answering, it's the same idea: tokens in my own context that later tokens attend to. I can't give exact implementation details of my own serving setup. But the overall structure (generate → stop at a tool call → harness runs it → call again with the result) is how tool use works for LLM agents in general, and it's visible in this thread.

So your analogy holds. Qwen3 in `generate_simple.py` is one thinking-out-loud loop. This thread is several such loops chained by an outer program that also fetches information in between.

**Q:** *It looks like theoretically responses with and without interruptions for tool use are equivalent - in terms of what is happening inside the transformer. By using a tool the model extracts some information from the external worls and continues thinking. When the reasoning happens without interruptions for tool usage, the model happens to "extract" additional information from its own weights so to speak. I am guessing that even computational performance is equivalent (except for caching) - each autogeneration step receives the entire prompt with additional text inserts (self-extracted or provided by tools). Tell me how correct is such interpretation?*

**Claude:** Your interpretation is mostly right, and the core of it is spot on. Two parts need correcting: the "equivalent computational cost" part, and to some degree the "extracting from weights" metaphor.

## What's right: the transformer doesn't tell them apart

Mechanically, tool output is just more tokens in the context. The forward pass is the same whether a token was sampled by the model, typed by you, or pasted in from a file by the harness. Attention sees one sequence and doesn't know where each piece came from. The only hint is the chat format: tool results are wrapped in role markers, like `<|im_start|>user` in Qwen's template. The model has *learned* to treat those sections differently (for example, as facts to rely on). That's learned behavior, not a different mechanism.

## Correction 1: tools add information, thinking adds computation

The metaphor of "extracting information from its own weights" is useful, but the two are not symmetric:

- **Self-generated tokens add no new information.** Everything in them is already determined by the weights plus the prompt (plus random sampling). What they add is **serial computation**. Each token is one more full pass through all the layers, and its result is written down where later tokens can read it. A fixed-depth network gets only so many layers "per thought". Writing tokens lets it chain many passes together. So "unfolding what's implicit in the weights" describes it better than "extracting".
- **Tool tokens add real information from outside**: the actual contents of `qwen3.py`, today's date, the result of running code. No amount of thinking produces these from the weights. When a model tries, that's hallucination. Tools can also hand off computation the model does badly, such as exact arithmetic.

There's a smaller difference too. The model's own tokens are **sampled**: it chose them, and could have chosen differently. Tool tokens are **forced**. They're fixed text the model has to condition on, whether or not it would have "expected" them.

## Correction 2: the cost is not equivalent, and prefill vs. decode is the big gap

This ties directly to Chapter 2. Two separate points:

**a) With a KV cache, each step does *not* reprocess the whole prompt.** Logically every new token attends to the entire context. Computationally, with the cache, each decode step runs only the **one new token** through the network and reads the stored keys/values for everything else. The version that literally re-feeds the whole sequence every step is the naive, uncached generator. Its growing cost is exactly what you measured when you compared the cached and uncached speeds in ch02.

**b) Inserted text is processed in parallel, generated text one token at a time.** In `generate_text_basic_stream_cache`, the prompt goes through the model in **one forward pass** covering all its tokens at once (prefill), and only then does the token-by-token loop begin (decode). Tool output is handled like prompt text. When a 1,000-token file comes back, it goes in as one parallel prefill pass, not 1,000 sequential steps. Decoding is slow because every token needs its own trip through all the weights, and that trip is limited by memory bandwidth. So:

| | Self-generated tokens (thinking) | Inserted tokens (tool output, prompt) |
|---|---|---|
| Processing | Sequential decode, one pass per token | Parallel prefill, many tokens per pass |
| Cost per token | High | Low |
| Adds | Computation | Information |

Tool use does add its own overhead: the pause, running the tool, and a new API call. If the provider's cache has expired, the whole context has to be prefilled again. That's the caching exception you mentioned, and at long context lengths it matters.

## A refined version of your sentence

> Inside the transformer, tool output and self-generated reasoning are the same kind of thing: tokens in one growing context that later tokens attend to. They differ in *what* they add: thinking adds serial computation over what the weights already imply, and tools add information the weights don't have. They also differ in *cost*: generated tokens are produced one at a time, while inserted tokens are absorbed in parallel.

This contrast could make a good slide for your talk, and it fits Chapter 2 since both points come from the prefill/decode split and the KV cache.
