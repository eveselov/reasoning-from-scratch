# Notes on Chapter 2.

## Settings for venv problem

My first action in the project (after cloning the repo, and making sure that the virtual environment is 
properly initialized) was to try a simplest python code in one of existing notebooks. I added
a new cell at the top and tried to execute it. VsCode asked me to create a venv - total surprize
(I expected I already have it). This is what Claude explained to me:

>> That's the classic VS Code notebook symptom: the folder having a working .venv on disk doesn't mean VS Code's notebook UI is pointed at it — those are two separate things.

>> What's happening: There's no .vscode/settings.json in this repo, so nothing tells VS Code which interpreter is "the" one for this workspace. When you ran the cell, the notebook had no kernel selected yet, and VS Code's kernel-picker flow (especially with the newer "Python Environments" extension) defaults to offering "Create Environment" rather than silently discovering .venv/Scripts/python.exe — even though your uv-created venv (confirmed: Python 3.13, ipykernel already installed at .venv/Lib/site-packages) is perfectly usable.

So I added the missing settings file as per CLAUDE recommandation.

Maybe it is obvious for other readers, but this cost me some time to start the real work.

## pip vs uv controvercy

In the beginning of the book there is a very convincing and reasonable recommendation to use uv rather than pip: use "uv add package" instead of "pip install". The entire environment in trhe repository is uv-oriented. Nevertheles, the notebooks use pip:

```
#!pip install -r https://raw.githubusercontent.com/rasbt/reasoning-from-scratch/refs/heads/main/requirements.txt
```

Looks confusing and inconsistent. I was encouraged to learn about uv (which I did not know before), I apprecite it and I like it.
Now I am totally confused betwen pip and uv usage.

## Appendix G

Side looked at apendix G which promised an internal structure os "an agent".
Looks ver ineresting. Now I am torn - whether to dive into those agent, chat, chainlit(!),
or continue the primary flow of Chapter 2.

## Appendix C

Another pleasant free gift - a well documented Qwen3 source code (Raschka's own reimplementation)

## Exceptional Professionalism

This book is really a piece of art. Art of story telling, art of technical explanation, art of programming.
You can use the book from multiple perspectives - read it in paper, then switch to a notebook
from git, or create a copy/pasted fragment for a specific topic. And eny of it just works!
You can read the book. You can read
the notebooks. You can read the auxiliary code. You can run any of it. You can use full setup
from the cloned repository, or partial pip- or uv- installed packages. Raschka has thought 
of everything!
Knowing from my own developer's experience how difficult is to not just to come to an idea
but to make it really work and work reliably and work for different users - I see that Raschka's
task would be impossible to implement. But here it is. Working! 

## Highly Struictural Guidance

The content of the entire book is systematically illustrated with visualdiagrams representing the overall context, previous and following steps. It is very easy to follow the plot and structure.

