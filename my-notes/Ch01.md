# Chapter 1. Understandinf reasoing models

**My own notes; not for Amazon review. See Amazon review in review/notes.md**

## On-going observations

The book roadmap: (1) load pretrained LLM, (2) implement evaluation technicque, (3) implement reasoning enhancing technicques without re-training, (4) implement reasoning enhancing techniques via training. *Ask Claude about this*: Before staring this book I expected that by "reasoning" models the authour will present technicques of building agents around LLMs (comething which he seems has done in APpendix G, which I haven't yet read). Apparently it was probably my lack of undestanding. 1-2-3-4 are the necessary basic steps needed to be understood, and the agents is something higher-level and not that fundamental... We'll see after consuming the book.

"Basic LLM is pattern recognition; reasoning ia an agent that breaks a task into steps and recover from mistakes". The second part is what I expected, the first part seems oversimplistic to me. But probably is most essential part of LLM. *Ask Claude about this*.

"Hand-on, code first approach" - this was the best in the previous book; and what I am expecting and value in this one.

"Reasoning refers o a model generating intemediate steps before producing its final answer" - simale and clear no BS definition of a complex concept.

"Chain-of-though reasoning differs from tradtional deterministic reasoning" - another example of a simple and lexr concept deifnition (deterministic reasoning that followsstrict, rule based steps). "LLM reasons autoregressively based on statistic patterns." This is where the "pattern recognition" becomes more clear - he is assumes reasoning patterns here as opposite toaxiomatic ddterministic algorithms.

preraining, post-training (aka fine-tuning), mid-training - *Ask Claude about this*: the distinction is no deeply phylosophical, just technical training convenience and resource management. Isn't it? Pretraining provides most basic knowledge and general reasoning, while post-training is specialization - roughly like evolutionary acquired vs shool-developed skills in humans. Isn't it?

"Instructiojn-tuned model is not yet a chatbot". Ochestration mst be adeded (system prompts, chat history, tool orchestration). *TODO:* learn appendix G as an example of such implementation. Might be a topic for a separate presentation lecture.

Three different methods for enhancing reasoning:
- Inference-compute scaling (chain-of-thought, sampling), fixed weights
- Reinforcement learning - trial-and-error based on the feedback from environment or human
- Distillation - from larger ("teacher") to simpler ("student") models (supervised fine tuning)

Two modes of reasoning: in closed-world and in open-world.

GPT-4.5: know when to think for a long time or not - yet another essential feature of reasoning. "Reasoning is not always necessary or desirable."

"Humans can engage in deterministic reasoning" - *SIDE IDEA:* why don't LLMs do the same on their own, not via explicitly programmed agents.

*SIDE IDEA:*: by letting the model to generate longer output we allow more reasoning iterations in the same initial context. Intermediary conclusions can be passer from previous forward passes to the following in form of newly generated tokens. This might extend the recurrent depth of the transformer beyond its natural fixed-leyers structure. Previously I though that one act of LLM inference has limited recurent thinking capacity. But now I noticed that, that since a single "inferrence" is technically an unlimited multi-forward pass process, the limitation can be overcome. Not even talking about multiple different prompts for the same task as in agents.

## Reflection in Anima Context

Current pretraining doesn't really separate "general reasoning/learning
ability" from "specific facts" the way evolution arguably does with
individuals — an LLM's pretraining bakes huge amounts of concrete
knowledge into the same weights that presumably also encode whatever
counts as its reasoning ability. In my personal forward-looking opinion
I expect that future pretraining approaches will move
toward instilling general reasoning/learning capacity while pushing
more of the specific-fact burden onto external, updatable sources
(retrieval, tools) — a direction current work like retrieval-augmented
generation and model/knowledge editing is already probing, even though
no one has fully cracked the separation yet.
